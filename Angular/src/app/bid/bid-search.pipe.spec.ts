import { BidSearchPipe } from './bid-search.pipe';

describe('BidSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BidSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
